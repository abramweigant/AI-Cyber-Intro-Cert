*.zip

# macOS
.DS_Store

# Jupyter runtime artifacts (local authoring only -- never ship these)
.ipynb_checkpoints/
.jupyter/
.jupyter_ystore.db
*.ystore.db

# INSTRUCTOR MATERIALS -- THIS REPO IS PUBLIC.
# Solutions live in the private repo under instructor/ (its own git repo).
# Never remove these two lines.
instructor/
Module5_Instructor.ipynb
# Any PDF render of the instructor copy carries the solutions with it.
*.pdf

# Model artifacts produced by the final lab (regenerated by running it)
*.keras
backdoor_detector_meta.json

# CSVs written by the Module 1 lab
first_100_urls.csv
with_index.csv
